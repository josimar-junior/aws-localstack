spring:
  application:
    name: aws

cloud:
  aws:
    stack:
      auto: false
    region:
      static: us-east-1
    credentials:
      access-key: josimar
      secret-key: josimar
    endpoint: http://localhost:4566
